Wire protocol — ``p5rem.protocol``
===================================

The wire protocol uses CBOR-framed messages over the SSH stdio transport.
The message types and framing helpers are documented here for reference;
normal users never need to call these directly.

.. automodule:: p5rem.protocol
   :members:
   :undoc-members:
